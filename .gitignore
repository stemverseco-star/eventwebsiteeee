# macOS / OS cruft
.DS_Store
**/.DS_Store
__MACOSX/

# Original hero video — 120MB HEVC .mov, unused (the site plays stemverse.mp4).
# Exceeds GitHub's 100MB per-file limit, so it is excluded from the repo.
pictures/stemverse.mov

# Vercel
.vercel

# Local test artifacts
.playwright-mcp/
*.log

.vercel
